Open Prisma Studio to browse the database.

Steps:

1. Check that DATABASE_URL is set in .env
2. Run `npx prisma studio` in the background
3. Report the URL (usually http://localhost:5555)
4. Remind user to stop the process when done
